import {createBrowserHistory} from "history";

// creates an object that React Router can use that shows what it
// is an what is was in the past.  Creates a history instance that all the 
// other files can use. 
export const history = createBrowserHistory();
